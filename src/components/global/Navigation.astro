<section>
  <div class="flex p-12 fixed top-0 z-50">
    <div class="flex flex-col text-3xl" id="top-left">
      <a href="https://github.com/Tnixc" target="_blank">🡔GitHub</a>
      <a href="https://twitter.com/_Tnixc" target="_blank">🡔X/Twitter</a>
      <button class="inline">▤Discord: @Tnixc</button>
    </div>
  </div>
</section>
<style>
  #top-left {
    transform: perspective(1000px) rotateY(-24deg) rotateX(24deg);
    a,
    button {
      position: relative;
      transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
      width: fit-content;
    }
    a:hover,
    button:hover {
      transform: scale(1.1);
    }
    a::after,
    button::after {
      content: "";
      position: absolute;
      inset: 0;
      width: 0%;
      background: white;
      mix-blend-mode: difference;
      transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }
    a:hover::after,
    button:hover::after {
      width: 100%;
    }
  }
</style>
